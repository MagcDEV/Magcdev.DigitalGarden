Is the building of data and methods that operates on that data into a single separated unit, known as a class, this class groups related data and behavior together making the code more structured and easy to work with.

An important aspect of encapsulation is **information hiding (Access control)** in csharp for example we use access modifiers to control access to classes and methods.